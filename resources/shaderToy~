void main( out vec4 fragColor, in vec2 fragCoord )
{
    /*vec2 q = fragCoord.xy / iResolution.xy;
    vec4 original = texture2D( iChannel0, vec2(q.x,q.y));
    float disX = q.x - iMouse.x/iResolution.x;
    float disY = q.y - iMouse.y/iResolution.y;
    float dis = sqrt(disX*disX + disY*disY);
    original /= (dis * 30.0) + 0.7; 
    fragColor = original;*/
}
